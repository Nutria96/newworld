const cache=require("./_lib/cache"),{checarLimite}=require("./_lib/rateLimit"),{json,method}=require("./_lib/http");
exports.handler=async(event)=>{const bad=method(event,"POST");if(bad)return bad;let body;try{body=JSON.parse(event.body||"{}");}catch{return json(400,{error:"JSON inválido"});}const question=String(body.mensaje||"").trim(),session=String(body.sessionId||"anon").slice(0,128);if(!question||question.length>18000)return json(400,{error:"Mensaje requerido (máximo 18000 caracteres)"});const limit=checarLimite(session);if(!limit.permitido)return json(429,{error:"Límite de sesión alcanzado",...limit});const simple=cache.intentarCalculoSimple(question),saved=cache.get(question);if(simple||saved)return json(200,{respuesta:simple||saved,cache:!!saved,restantes:limit.restantes});const key=process.env.AI_API_KEY;if(!key)return json(503,{error:"Chat no configurado"});const provider=process.env.AI_PROVIDER||"deepseek",base=provider==="openai"?"https://api.openai.com/v1":"https://api.deepseek.com/v1",model=process.env.AI_MODEL||(provider==="openai"?"gpt-4.1-mini":"deepseek-chat");const system="Sos Chongoku en Modo Sabio. Respondé en español claro, completo, directo y seguro. No titubees ni repitas preguntas. Si falta información, indicá exactamente qué dato falta. Ayudá con programación, creación y producción musical sin afirmar que ejecutaste, viste o analizaste algo que no recibiste. El contexto local puede contener preferencias e historial; úsalo sin decir que entrenaste o aprendiste permanentemente. No inventes fuentes, enlaces ni resultados.";const r=await fetch(`${base}/chat/completions`,{method:"POST",headers:{authorization:`Bearer ${key}`,"content-type":"application/json"},body:JSON.stringify({model,messages:[{role:"system",content:system},{role:"user",content:question}],max_tokens:900})});if(!r.ok)return json(502,{error:"El proveedor de IA no respondió"});const data=await r.json(),answer=data.choices?.[0]?.message?.content?.trim();if(!answer)return json(502,{error:"Respuesta vacía del proveedor"});cache.set(question,answer);return json(200,{respuesta:answer,restantes:limit.restantes});};
