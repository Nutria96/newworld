# Estado de la red CHONGSEB

Reporte pendiente de medici?n local.

Ejecuta:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\diagnostico_red.ps1
```

El script reemplazar? este archivo con latencia, p?rdida, salida opcional de Speedtest, vecinos IPv4 observados y resumen de canales WiFi. No confundas los vecinos vistos por Windows con la lista completa de clientes del router.

Antes de compartir el resultado, elimina direcciones IP, MAC, nombres de interfaces u otros datos privados.
